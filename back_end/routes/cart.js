// const cartController = require('../controllers/Cart')
// const {verifyToken,verifyAdmin} = require("../middleware/auth")
// const express = require("express");
// const router = express.Router();
//
// router.post("/", verifyToken, cartController.addCart);
// router.put("/:id", verifyToken, cartController.updatedCart);
// router.delete("/:id", verifyToken, cartController.deleteCart);
// router.get("/find/:userId", verifyToken, cartController.getUserCart);
// router.get("/", verifyToken, verifyAdmin, cartController.findCart);
//
// module.exports = router;